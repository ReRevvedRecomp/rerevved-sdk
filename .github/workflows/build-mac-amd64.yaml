name: Build rexglue-sdk mac-amd64

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    uses: ./.github/workflows/_build-platform.yaml
    with:
      platform: mac-amd64
      ref: ${{ github.sha }}
      publish_mode: release
    secrets: inherit
