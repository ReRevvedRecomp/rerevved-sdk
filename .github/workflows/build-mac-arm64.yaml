name: Build rexglue-sdk mac-arm64

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    uses: ./.github/workflows/_build-platform.yaml
    with:
      platform: mac-arm64
      ref: ${{ github.sha }}
      publish_mode: release
    secrets: inherit
